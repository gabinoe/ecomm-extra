const initialState = {
    // Initial state for categories
  };
  
  export const categoryReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define actions to handle category state changes
      default:
        return state;
    }
  };
  
  // src/store/reducers/productReducer.js
  const initialState = {
    // Initial state for products
  };
  
  export const productReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define actions to handle product state changes
      default:
        return state;
    }
  };
  
  // src/store/reducers/tagReducer.js
  const initialState = {
    // Initial state for tags
  };
  
  export const tagReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define actions to handle tag state changes
      default:
        return state;
    }
  };
  